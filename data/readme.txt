First, install required packages:
    pip install numpy pandas scikit-learn requests

Initialize the database:
    python database_setup.py

Run the main program:
    python main.py

Future optimizations that need to be made:
    - be able to search by id, or honestly any other value to make changes for all events of that type or only a certain event
    - connect to actual sql database they have